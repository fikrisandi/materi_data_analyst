# Latihan — Python Functions & Modules

## Soal 1 — Function Basic
Bikin function `format_rupiah(nominal)` yang convert `8500000` → `"Rp 8.500.000"`.

## Soal 2 — Default Args
Bikin function `hitung_total_diskon(total, persen=10)` dengan default 10%.

## Soal 3 — Multiple Return
Bikin function `stat_summary(numbers)` yang return `(min, max, avg, count)`.

## Soal 4 — Lambda + Sort
Sort list of dict pelanggan by umur descending.

## Soal 5 — Higher-order
Pakai `filter()` untuk ambil transaksi > 50000.

## Soal 6 — List Comprehension Kompleks
Dari list transaksi, buat dict `{kategori: total_revenue}`.

## Soal 7 — Bikin Module
Bikin `utils.py` dengan 3 function utility (rupiah, kategori, validasi). Import & pakai di `main.py`.

## Soal 8 — Refactor
Code monolithic di starter, refactor jadi 3 function dengan single responsibility.

(Detail starter & test di `code/latihan-starter.ipynb`)
